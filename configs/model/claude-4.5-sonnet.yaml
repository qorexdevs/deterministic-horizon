# Claude-4.5-Sonnet Model Configuration
# =====================================

model:
  name: claude-4.5-sonnet
  provider: anthropic
  model_id: claude-sonnet-4-5-20250929

  # Generation parameters
  temperature: 0.0
  max_tokens: 8192
  top_p: 1.0
  
  # API settings
  api_base: https://api.anthropic.com/v1
  timeout: 120
  max_retries: 3
  
  # Pricing (USD per 1K tokens, as of Jan 2026)
  pricing:
    input: 0.003
    output: 0.015
  
  # Capabilities
  capabilities:
    tool_use: true
    vision: true
    streaming: true
    system_prompt: true
    extended_thinking: false
